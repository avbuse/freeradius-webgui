I have a freeradius server installed onto an ubuntu server.  I want to create an easy to use dashboard, accessible via the web so that I can :

    - start and stop the service
    - configure the service (enabling / disabling the features)
    - update the relevant config files
    - show the recent logs
    - show a graph of successful / unsuccessful authentications
    - update the server certificate
    - update the trusted root certificates
    - changes inititate a process of syntax validation, pre-change backups, transactional apply, restart behavior, and one-click rollback criteria.
    - 

The web interface should be secured with username and passkey, potentially integrated with Entra-ID for login



